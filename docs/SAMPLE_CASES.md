# Sample Cases

## Current sample inputs

- `sample_cases/inputs/basic_starrocks.json`
- `sample_cases/inputs/partition_focus.json`

## Current sample outputs

Outputs are generated into:

- `sample_cases/outputs/`
- `sample_cases/transcripts/`

## How they are produced

At the moment, sample outputs are generated by a mock pipeline so the full workflow, review gates, logging, and output persistence can be tested before production prompts are filled in.

## Notes to fill later

- Add real-model sample cases:
- Add expected review feedback scenarios:
- Add database-execution validation scenarios:
